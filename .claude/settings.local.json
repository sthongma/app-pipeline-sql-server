{
  "permissions": {
    "allow": [
      "Bash(py -3 -m py_compile:*)",
      "Bash(python -m py_compile:*)",
      "Bash(tree:*)",
      "Bash(dir:*)",
      "Bash(python:*)",
      "Bash(git mv:*)",
      "Bash(git add:*)",
      "Bash(findstr:*)",
      "Bash(git commit -m \"$(cat <<''EOF''\nrefactor: Standardize to NVARCHAR(MAX) and implement metadata tracking system\n\n- Remove specific NVARCHAR length options (100, 255, 500, 1000) in favor of NVARCHAR(MAX) only\n- Replace updated_at with comprehensive metadata columns: _loaded_at, _created_at, _source_file, _batch_id, _upsert_hash\n- Implement MD5 hash-based upsert matching for improved performance and reliability\n- Remove string length validation (no longer needed with MAX length)\n- Add automatic index creation on _upsert_hash and _loaded_at columns\n- Clean up test files that are no longer needed\n- Add findstr to allowed Bash tools in Claude settings\n\nThis refactoring simplifies data type management and adds robust data lineage tracking capabilities.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nEOF\n)\")",
      "Bash(git commit -m \"$(cat <<''EOF''\nrefactor: Simplify data types to 5 core types and remove backward compatibility\n\nSimplify the type system to only support 5 essential SQL Server data types:\n- NVARCHAR(MAX) for all text\n- INT for whole numbers and booleans (0/1)\n- FLOAT for decimal numbers\n- DATE for dates\n- DATETIME for date/time values\n\nChanges:\n- Remove BIGINT, DECIMAL(18,2), BIT from SUPPORTED_DTYPES constant\n- Update type inference to map boolean → INT instead of BIT\n- Clean up type conversions in data_processor_service.py (remove DECIMAL, SmallInteger, Boolean)\n- Clean up type handling in data_upload_service.py (remove SmallInteger, DECIMAL, Boolean conversions)\n- Remove backward compatibility: validators now accept only exact matches from SUPPORTED_DTYPES\n- Update validators to remove unused type patterns (VARCHAR, DECIMAL, CHAR)\n- Remove unused imports across all services\n- Update README.md examples to use simplified types\n- Boolean validator now returns empty list (BIT type no longer supported)\n- Numeric validator now only handles INT and FLOAT\n\nBreaking Changes:\n- Old configurations with NVARCHAR(255), NVARCHAR(500), DECIMAL(18,2), BIGINT, or BIT will no longer be accepted\n- Existing databases with these types cannot be read by the system\n- Boolean data must now be stored as INT (0/1) instead of BIT\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nEOF\n)\")"
    ],
    "deny": [],
    "ask": []
  }
}
